# System deps are installed by install.sh (ffmpeg, git-lfs).
# Python deps below. torch/torchaudio come from the cu128 index (see install.sh).

transformers==4.57.6
huggingface-hub==0.36.2
safetensors==0.7.0
tiktoken==0.12.0
numpy==2.2.6
soundfile==0.13.1
scipy==1.18.1
accelerate==1.13.0
demucs==4.1.0
torchaudio==2.10.0
